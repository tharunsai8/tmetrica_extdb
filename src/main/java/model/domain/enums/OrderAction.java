package model.domain.enums;

/**
 * The enum Order action.
 */
public enum OrderAction {
    /**
     * Join order action.
     */
    JOIN,
    /**
     * Create order action.
     */
    CREATE,
    /**
     * Delete order action.
     */
    DELETE
}
